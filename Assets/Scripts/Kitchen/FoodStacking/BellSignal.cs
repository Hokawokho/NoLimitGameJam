using UnityEngine;

public class BellSignal : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //public static class BellSignals
    //{
    //    public static event System.Action OnFinishBell;
    //    public static void RaiseFinishBell() => OnFinishBell?.Invoke();
    //}

}
