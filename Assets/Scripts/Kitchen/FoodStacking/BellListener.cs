using UnityEngine;

public class BellListener : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    //[SerializeField] Animator animator;
    //static readonly int FinishBell = Animator.StringToHash("FinishBell");
    

    //void OnEnable() => BellSignals.OnFinishBell += Handle;
    //void OnDisable() => BellSignals.OnFinishBell -= Handle;

    //void Handle() => animator.SetTrigger(FinishBell);
    
}
